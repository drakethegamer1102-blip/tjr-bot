# Core data/math
numpy>=1.26,<3
pandas>=2.2,<3
PyYAML>=6.0

# Broker + market data
alpaca-py>=0.20

# Real futures data (ES=F) for the daily futures strategies + backtests
yfinance>=0.2.40

# Notifications (Telegram via simple HTTP calls)
requests>=2.31

# .env loading (review_bot imports it; nightly CI crashed while it was missing)
python-dotenv>=1.0

# Stats dashboard
Flask>=3.0

# Dev / tests
pytest>=8.0
